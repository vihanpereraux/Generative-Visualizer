var linkArray = ["aaa", "bbb", "ccc"];
console.log(linkArray[0]);

for (let ind = 0; ind < array.length; ind++) {
    const element = array[ind];
    
}

document.getElementById("links").innerHTML = "Links added";